{
  "name": "llm-extract-evals",
  "version": "0.1.0",
  "description": "Schema-validated LLM extraction from grid interconnection documents, with an eval harness (field accuracy, schema-valid rate, failure taxonomy).",
  "type": "module",
  "scripts": {
    "extract": "bun run src/run.ts",
    "eval": "bun run src/eval.ts",
    "batch": "bun run src/batch.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "bun-types": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
