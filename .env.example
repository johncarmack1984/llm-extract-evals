# Copy to .env and fill in. .env is gitignored -- never commit a real key.
ANTHROPIC_API_KEY=
# Extraction model. Default is the most capable; swap to a cheaper tier for
# fast eval sweeps: claude-sonnet-5 or claude-haiku-4-5.
MODEL=claude-opus-4-8
